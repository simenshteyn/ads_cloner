import 'package:flutter/material.dart';

class ClonePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Clone Ads'),
      ),
      body: Text('Some testing'),
    );
  }
}
